import styled from 'styled-components';
import SpanProto from '../../atoms/span';

export const Container = styled(SpanProto)`
  position: relative;
`;

export const Span = styled(SpanProto)`
  position: absolute;
`;