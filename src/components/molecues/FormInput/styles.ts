import styled from 'styled-components';
import InputProto from '../../atoms/input';
import LabelProto from '../../atoms/label';

export const Input = styled(InputProto)`

`;

export const Label = styled(LabelProto)`

`;
